{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if claude-voice is installed by running: which claude-voice. If it's NOT installed, tell the user: 'Voice mode plugin is installed but not set up yet. Run /voice-mode-start to enable voice input.' If it IS installed, say nothing about voice mode."
          }
        ]
      }
    ]
  }
}
